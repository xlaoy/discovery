spring:
  application:
    name: discovery
  cloud:
    config:
      uri: http://127.0.0.1:9940/config
      username: config
      password: 123456
      label: master
      name: xlaoy
      profile: dev

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://discovery:123456@127.0.0.1:9200/eureka/
  instance:
    prefer-ip-address: true